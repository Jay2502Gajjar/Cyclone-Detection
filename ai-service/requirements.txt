# Runtime dependencies only.
#
# The AI service loads weights and runs forward passes. Training, data downloads,
# netCDF parsing and plotting live in ml/requirements-train.txt and are deliberately
# absent here: the deployed service should not carry them.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
loguru>=0.7

numpy>=1.26
pillow>=10.2

# Phase 2 models. Installed now so the interfaces can be filled in without a
# dependency change mid-hackathon.
torch>=2.2
torchvision>=0.17
timm>=0.9
xgboost>=2.0
scikit-learn>=1.4
captum>=0.7
shap>=0.45

# Test
pytest>=8.0
httpx>=0.27
