# API documentation

Superseded. The API contract lives at the repository root, in
[`API_CONTRACT.md`](../../API_CONTRACT.md), so the three services that implement it all
point at one file.

Live documentation while the stack is running:

- Swagger UI — http://localhost:8090/swagger-ui.html
- FastAPI docs — http://localhost:8000/docs
